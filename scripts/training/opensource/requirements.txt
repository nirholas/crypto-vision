# Python dependencies for open-source model fine-tuning
# Install in the training Docker image or GPU environment
#
# Usage: pip install -r requirements.txt
#
# For GPU environments with CUDA 12.4:
#   pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt

# Core ML framework (install torch separately with CUDA index)
# torch==2.4.0

# HuggingFace ecosystem
transformers==4.44.0
datasets==2.21.0
accelerate==0.33.0
peft==0.12.0
trl==0.9.6

# Memory-efficient training
bitsandbytes==0.43.3

# Tokenizer dependencies
scipy
sentencepiece
protobuf

# Quantization
auto-gptq==0.7.1
autoawq==0.2.6

# GCS upload / model management
google-cloud-storage

# Inference server (for post-training smoke tests)
vllm>=0.5.0

# Benchmarking
aiohttp>=3.9.0

# Optional: experiment tracking (uncomment as needed)
# wandb
# tensorboard
